namespace YarnGenerator
{
    public static class KnitSettings
    {
        // # of radial points when rendering yarn as cylinder
        public static int radialRes = 8;

        // # of segments per stitch in x-direction when rendering yarn
        public static int stitchRes = 16;

        // Stitch height - controls aspect ratio of stitches
        public static float stitchHeight = 1.2f;

        // Stitch height - controls aspect ratio of stitches
        public static float stitchWidth = 1.0f;
    }
}